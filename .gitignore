# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# env / secrets — NUNCA commitar
.env
.env*.local
.env.local
.env.development.local
.env.production.local

# backups do banco — contêm e-mails, hashes de senha e todo o PII dos
# usuários. Um dump commitado é vazamento de base inteira. NUNCA commitar.
/backups/
*.sql.gz
*.dump

# misc
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# criativos de campanha (vídeos de 100MB+ — acima do limite do GitHub) e
# screenshots de revisão da landing. Ficam só no disco local.
/ads
/public/screenshots
